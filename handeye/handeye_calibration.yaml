# Hand-Eye Calibration Result
# Transform from end-effector frame to camera frame (T_ee_cam)
#
# Usage: P_base = T_base_ee × T_ee_cam × P_cam
# Where:
#   T_base_ee = Forward kinematics from robot joint angles
#   T_ee_cam  = This calibration (fixed - camera is mounted on gripper)
#   P_cam     = Point in camera coordinates

transformation:
  # 3x3 rotation matrix (camera orientation relative to end-effector)
  rotation:
    - [0.0153, -0.6245, 0.7809]
    - [-0.9998, -0.0160, 0.0068]
    - [0.0083, -0.7809, -0.6246]
  
  # Translation vector [x, y, z] in meters
  # Camera position relative to end-effector origin
  translation: [-0.0361, 0.0085, 0.0724]
  
  # Quaternion (x, y, z, w) - same rotation as matrix
  quaternion_xyzw: [0.6458, -0.6331, 0.3076, -0.3052]

metadata:
  description: "A1X eye-in-hand calibration (camera on gripper)"
  timestamp: '2026-02-09T23:26:20'
  num_poses: 17
  marker_id: 42
  marker_size_m: 0.1
  method: TSAI
